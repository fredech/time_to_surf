<div class="index-search">
  <%= render 'shared/searchbar_index'%>
</div>
<div class="main-container">
  <div class="map-container index-map" >
    <%= render 'shared/map', markers: @markers%>
  </div>
  <div class="card-container">
    <%= render 'shared/card', spot: @selected_spots[2], rank: 1 %>
    <%= render 'shared/card', spot: @selected_spots[1], rank: 2 %>
    <%= render 'shared/card', spot: @selected_spots[0], rank: 3 %>
    <!-- <#%= render 'shared/card_new', spot: spot, conditions_rates: conditions_rate(spot, "12h"), matching_rates: matching_rate(spot, current_user, "12h") %> -->
  </div>
</div>
