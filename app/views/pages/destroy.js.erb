// console.log('coucou')

// function deletePreferredSpot() {
//   const preferred_spots = document.querySelectorAll("data-spot='<%= @spot.id %>'");
//   preferred_spots.forEach((spot) => {
//     spot.innerHTML = "<%= j render 'shared/preferred_spot', spot: @spot %>"
//   });

// }


// deletePreferredSpot();
